#!/bin/bash

# Script to add user unlocker

logfile=$(basename $0)-$(date %F_%T).log

mkdir -p /home2 2>&1 | tee $logfile

adduser --home /home2/unlocker --gecos "" unlocker  2>&1 | tee -a $logfile

adduser jwheaton sudo

# vim:ts=4:sts=4:sw=4
